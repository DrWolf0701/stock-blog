# 📧 macOS 郵件 App 設定指南
## 中央大學校友信箱設定

### 🔧 伺服器設定資訊
| 項目 | 設定值 |
|------|--------|
| **信箱類型** | POP |
| **電子郵件地址** | `a102456014@alumni.ncu.edu.tw` |
| **密碼** | `Penthouse0701` |
| **使用者名稱** | `a102456014@alumni.ncu.edu.tw` |
| **收件伺服器** | `pop3.alumni.ncu.edu.tw` |
| **收件連接埠** | `995` |
| **收件加密** | SSL |
| **發件伺服器** | `smtp.alumni.ncu.edu.tw` |
| **發件連接埠** | `587` (TLS) 或 `465` (SSL) |
| **發件加密** | TLS 或 SSL |

### 📱 設定步驟

#### 步驟1：開啟「郵件」App
1. 打開「應用程式」資料夾
2. 點擊「郵件」圖示
3. 如果第一次使用，會自動跳出設定畫面
4. 如果已有帳號，點選「郵件」→「新增帳號」

#### 步驟2：選擇帳號類型
1. 選擇「其他郵件帳號...」
2. 點擊「繼續」

#### 步驟3：輸入帳號資訊
```
名稱：你的姓名（顯示用）
電子郵件地址：a102456014@alumni.ncu.edu.tw
密碼：Penthouse0701
```
點擊「登入」

#### 步驟4：手動設定（如果自動偵測失敗）
如果自動設定失敗，選擇「手動設定」：

**收件郵件伺服器設定：**
```
帳號類型：POP
收件郵件伺服器：pop3.alumni.ncu.edu.tw
使用者名稱：a102456014@alumni.ncu.edu.tw
密碼：Penthouse0701
連接埠：995
加密：SSL
```

**寄件郵件伺服器設定：**
```
寄件郵件伺服器：smtp.alumni.ncu.edu.tw
使用者名稱：a102456014@alumni.ncu.edu.tw  
密碼：Penthouse0701
連接埠：587（先試這個）
加密：TLS
```
或嘗試：
```
連接埠：465
加密：SSL
```

#### 步驟5：完成設定
1. 點擊「登入」
2. 等待驗證完成
3. 選擇要同步的項目（郵件、備忘錄、行事曆等）
4. 點擊「完成」

### ⚙️ 進階設定建議

#### 收件設定優化
```
✓ 在伺服器保留郵件副本：建議開啟
✓ 移除伺服器上的副本：下載後立即移除（不建議）
✓ 移除已下載的郵件：從伺服器移除（謹慎使用）
✓ 下載附件：自動
```

#### 發件設定
```
✓ 預設帳號：設為此帳號
✓ 寄件時包含原始郵件：依需求設定
✓ 傳送時自動加密副本：建議開啟
```

### 🔍 疑難排解

#### 常見問題1：無法收信
```
可能原因：
1. 連接埠錯誤（應為995）
2. SSL設定錯誤
3. 防火牆阻擋
解決方案：
• 確認連接埠為995
• 確認加密為SSL
• 暫時關閉防火牆測試
```

#### 常見問題2：無法發信
```
可能原因：
1. SMTP連接埠錯誤
2. 加密方式錯誤
3. 需要驗證
解決方案：
• 嘗試連接埠587+TLS
• 嘗試連接埠465+SSL
• 確認「寄件伺服器需要驗證」已勾選
```

#### 常見問題3：連線超時
```
可能原因：
1. 網路問題
2. 伺服器維護
3. DNS設定
解決方案：
• 檢查網路連線
• 稍後再試
• 使用其他DNS（如8.8.8.8）
```

### 📊 測試連線指令

#### 終端機測試POP3：
```bash
openssl s_client -connect pop3.alumni.ncu.edu.tw:995 -crlf
# 連線後輸入：
USER a102456014@alumni.ncu.edu.tw
PASS Penthouse0701
STAT
QUIT
```

#### 終端機測試SMTP：
```bash
# 測試連接埠587
openssl s_client -connect smtp.alumni.ncu.edu.tw:587 -starttls smtp

# 測試連接埠465  
openssl s_client -connect smtp.alumni.ncu.edu.tw:465
```

### 🔒 安全建議

1. **定期更改密碼**
2. **啟用兩步驟驗證**（如果學校提供）
3. **不要在其他裝置儲存密碼**
4. **使用強密碼**
5. **定期檢查登入記錄**

### 📞 技術支援

如果遇到問題：
1. **中央大學計資中心**：可能有校友信箱支援
2. **macOS 郵件支援**：蘋果官方文件
3. **網路管理員**：檢查網路設定

### ✅ 設定完成檢查清單
- [ ] 可以收信
- [ ] 可以發信
- [ ] 附件可正常下載
- [ ] 郵件同步正常
- [ ] 沒有連線錯誤

---

*最後更新：2026-02-14*
*此文件僅供設定參考，不儲存任何帳號密碼*